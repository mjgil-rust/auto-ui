# macOS Support Implementation Tasks

This task list tracks every item required to implement the macOS support design in `docs/macos-support-design.md`. Each task must have tests and documentation before it can be marked **Complete**.

---

## Phase 1 — Trait Extraction & X11 Refactor

| No. | Task | Status | Required Tests Before Complete | Required Documentation Before Complete | Crate Design |
|-----|------|--------|-------------------------------|----------------------------------------|--------------|
| 1 | Move `WindowDriver` trait from `auto-ui-driver-x11` to `auto-ui-core` | Complete | Unit tests proving trait compiles in `auto-ui-core` without driver dependencies; test that downstream crates can import it | `auto-ui-core` API docs must describe the trait contract; DESIGN.md must be updated to show trait location | Create `auto-ui-core/src/window_driver.rs` containing `WindowDriver`, `WindowGeometry`, `VisualMetric`, and `heuristic_text_visible` |
| 2 | Expand `WindowDriver` trait to cover all current X11 free-function usage | Complete | Trait method count test: assert every old free function has a corresponding trait method; compile-test for `dyn WindowDriver` | Trait-level rustdoc for every method with parameter descriptions and error conditions | Add methods: `check_required_tools`, `find_windows`, `find_windows_for_pid`, `find_window`, `find_window_for_pid`, `wait_for_new_window`, `get_active_window`, `get_window_pid`, `get_geometry`, `resize`, `set_geometry`, `activate`, `lower`, `background`, `window_exists`, `press_key`, `type_text`, `clear_search`, `select_session`, `screenshot`, `crop_metric`, `image_size` |
| 3 | Move `heuristic_text_visible` to `auto-ui-core` as a standalone function | Complete | Unit test with boundary values (stddev 0.0, 0.01, colors 0, 16) asserting true/false outcomes | rustdoc explaining the heuristic thresholds and when it may fail | Place in `auto-ui-core/src/window_driver.rs` |
| 4 | Create `X11WindowDriver` struct in `auto-ui-driver-x11` implementing `WindowDriver` | Complete | Struct implements trait compile-test; `FakeWindowDriver` updated to use trait from `auto-ui-core` | rustdoc on `X11WindowDriver` describing that it wraps `xdotool`/`wmctrl` shell commands | Add `pub struct X11WindowDriver; impl WindowDriver for X11WindowDriver { ... }` in `auto-ui-driver-x11/src/lib.rs` |
| 5 | Migrate all X11 free functions into `X11WindowDriver` methods | Complete | Each method tested via fake-command injection or subprocess mocking; at minimum test error paths when `xdotool` returns non-zero | Each method must have rustdoc with the underlying shell command it executes | `X11WindowDriver` delegates to existing free functions kept as crate-private helpers; public surface is strictly the trait impl |
| 6 | Update `FakeWindowDriver` to implement the expanded `WindowDriver` trait from `auto-ui-core` | Complete | Unit tests for every `FakeWindowDriver` method asserting state mutations in `FakeWindowState` | rustdoc explaining test-double behavior and state tracking | Update `auto-ui-driver-x11/src/fake_driver.rs` to import trait from `auto-ui-core`; add stubs for all new trait methods |
| 7 | Remove orphan `WindowDriver` trait definition from `auto-ui-driver-x11/src/window_driver_trait.rs` | Complete | CI compile test for entire workspace after file removal | DESIGN.md must be updated to remove stale file reference | Delete file; update `auto-ui-driver-x11/src/lib.rs` re-exports to pull from `auto-ui-core` |
| 8 | Move `WindowGeometry` and `VisualMetric` from `auto-ui-driver-x11` to `auto-ui-core` | Complete | Serialization round-trip tests for both structs in `auto-ui-core` | rustdoc with field descriptions | Place in `auto-ui-core/src/window_driver.rs`; remove duplicates from `auto-ui-driver-x11`; re-export from `auto-ui-driver-x11` for backward compat |
| 9 | Update `auto-ui-core/src/lib.rs` to export `WindowDriver` and related types | Complete | Compile test that external crates can access `auto_ui_core::WindowDriver` | Module-level rustdoc for `window_driver` module | Add `pub mod window_driver;` and re-exports in `lib.rs` |
| 10 | Ensure `cargo test --workspace` passes on Linux after Phase 1 | Complete | Full workspace test suite on `ubuntu-latest` CI runner; verified locally with `cargo check --workspace` and `cargo test -p auto-ui-core -p auto-ui-driver-x11` | N/A (existing CI docs suffice) | No new crates; purely refactor within existing workspace |

---

## Phase 2 — ImageMagick Elimination

| No. | Task | Status | Required Tests Before Complete | Required Documentation Before Complete | Crate Design |
|-----|------|--------|-------------------------------|----------------------------------------|--------------|
| 11 | Add `image` crate dependency to `auto-ui-core` | Complete | Lockfile diff test confirming resolution; feature gate test (`png` + `jpeg` enabled) | `Cargo.toml` comment explaining why `default-features = false` | `[dependencies.image]` in `auto-ui-core/Cargo.toml` with features `["png", "jpeg"]` |
| 12 | Implement Rust-native `image_size(path)` using the `image` crate | Complete | Unit tests with 1x1 PNG, 640x480 JPEG, and non-image file; assert correct dimensions or proper error | rustdoc with supported formats and error conditions | Function in `auto-ui-core/src/image_processing.rs` |
| 13 | Implement Rust-native `crop_metric(path, x, y, w, h)` using the `image` crate | Complete | Unit tests with known images: solid color (stddev≈0, colors=1), gradient (stddev>0.01, colors>16), and edge cases (crop larger than image) | rustdoc explaining grayscale conversion, standard deviation formula, and color-counting strategy | Function in `auto-ui-core/src/image_processing.rs`; uses pixel iteration with clamping |
| 14 | Remove `convert` and `identify` from `REQUIRED_X11_TOOLS` | Complete | Assert `REQUIRED_X11_TOOLS.len() == 3` (xdotool, wmctrl, import); assert `check_required_tools` no longer fails when `convert` is missing | Driver docs must state ImageMagick is no longer required | Update `auto-ui-driver-x11/src/lib.rs` constant and `check_required_tools` |
| 15 | Update `X11WindowDriver::crop_metric` to call Rust-native impl | Complete | Integration test: capture real PNG with X11 driver, crop, and assert metrics match ImageMagick baseline within tolerance | X11 driver rustdoc must note image processing is now native | `impl WindowDriver for X11WindowDriver { fn crop_metric(...) { auto_ui_core::image_processing::crop_metric(...) } }` |
| 16 | Update `X11WindowDriver::image_size` to call Rust-native impl | Complete | Same as task 15 but for image dimensions | Same as task 15 | Same delegation pattern as task 15 |
| 17 | Delete ImageMagick-specific unit tests that asserted shell command behavior | Complete | Confirm no tests reference `"convert"` or `"identify"` strings via grep | N/A | Clean up `auto-ui-driver-x11/src/lib.rs` test module |
| 18 | Run Linux live smoke tests to verify screenshot metrics still work | Incomplete | Live tests for `debug` and `header_debug` scenarios with `AUTO_UI_RUN_LIVE_TESTS=1` | README must be updated to remove ImageMagick from setup instructions | No code changes; validation only |
| 19 | Ensure `cargo test --workspace` passes on Linux after Phase 2 | Incomplete | Full workspace test suite on `ubuntu-latest` CI runner | N/A | Validation gate |

---

## Phase 3 — Adapter Decoupling

| No. | Task | Status | Required Tests Before Complete | Required Documentation Before Complete | Crate Design |
|-----|------|--------|-------------------------------|----------------------------------------|--------------|
| 20 | Remove `auto-ui-driver-x11` dependency from `auto-ui-adapter-rust-chatbot/Cargo.toml` | Complete | `cargo check` for `auto-ui-adapter-rust-chatbot` passes without `auto-ui-driver-x11` in its dep tree | Adapter crate docs must state it is driver-agnostic | Delete the `[dependencies.auto-ui-driver-x11]` line; added as `[dev-dependencies]` for test compilation |
| 21 | Remove `auto-ui-driver-x11` dependency from `auto-ui-adapter-gpui/Cargo.toml` | Complete | `cargo check` for `auto-ui-adapter-gpui` passes without `auto-ui-driver-x11` in its dep tree | Adapter crate docs must state it is driver-agnostic | Delete the `[dependencies.auto-ui-driver-x11]` line; added as `[dev-dependencies]` for test compilation |
| 22 | Update `auto-ui-adapter-rust-chatbot` entry points to accept `&dyn WindowDriver` | Complete | Compile tests for `run_debug`, `run_header_debug`, `run_prompt_debug` with `&FakeWindowDriver` | Each public function must document the `driver` parameter | Change signatures in `adapter/debug.rs`, `adapter/header_debug.rs`, `adapter/prompt_debug.rs` |
| 23 | Replace all `x11::` free-function calls in rust-chatbot `debug.rs` with `driver.` method calls | Complete | Fake-driver integration test exercising `run_debug` without any `xdotool` subprocesses | Inline comments for complex window-manipulation sequences | Mechanical refactor: `x11::find_window_id(...)` → `driver.find_window(...)` etc. |
| 24 | Replace all `x11::` free-function calls in rust-chatbot `header_debug.rs` with `driver.` method calls | Complete | Fake-driver integration test exercising `run_header_debug` | Same as task 23 | Same mechanical refactor |
| 25 | Replace all `x11::` free-function calls in rust-chatbot `prompt_debug.rs` with `driver.` method calls | Complete | Fake-driver integration test exercising `run_prompt_debug` | Same as task 23 | Same mechanical refactor |
| 26 | Replace all `x11::` free-function calls in GPUI `run_scroll_matrix` with `driver.` method calls | Complete | Fake-driver test for `run_scroll_matrix` with `capture_window = true` | Same as task 23 | Mechanical refactor in `auto-ui-adapter-gpui/src/lib.rs` |
| 27 | Replace all `x11::` free-function calls in GPUI `run_scrollbar_trace` with `driver.` method calls | Complete | Fake-driver test for `run_scrollbar_trace` | Same as task 23 | Same mechanical refactor |
| 28 | Replace all `x11::` free-function calls in GPUI `run_conversation_paint` with `driver.` method calls | Complete | Fake-driver test for `run_conversation_paint` | Same as task 23 | Same mechanical refactor |
| 29 | Update `run_process_with_optional_capture` to accept `&dyn WindowDriver` | Complete | Test proving `background_window` and `screenshot` are called through the driver, not directly | Function rustdoc must describe driver usage | Update signature in `auto-ui-adapter-gpui/src/lib.rs` |
| 30 | Update `auto-ui-cli` to construct `X11WindowDriver` and inject into adapters | Complete | Compile test on Linux with `X11WindowDriver` instantiation; test that unsupported target still errors correctly | CLI docs must describe platform-specific driver selection | Update `auto-ui-cli/src/run_cmd.rs` `run()` function; add `build_driver()` helper |
| 31 | Update `auto-ui-cli/src/debug_cmd.rs` to inject driver into `rust_chatbot::run_debug` | Complete | CLI compile test with debug command | Command docs must remain current | Pass `&*driver` to adapter function |
| 32 | Update `auto-ui-cli/src/header_debug_cmd.rs` to inject driver into `rust_chatbot::run_header_debug` | Complete | CLI compile test with header-debug command | Command docs must remain current | Pass `&*driver` to adapter function |
| 33 | Add conditional `[target.'cfg(target_os = "linux")'.dependencies]` for `auto-ui-driver-x11` in `auto-ui-cli/Cargo.toml` | Complete | `cargo check --workspace` on Linux still resolves `auto-ui-driver-x11` | Cargo.toml comment explaining platform gating | Update `crates/auto-ui-cli/Cargo.toml`; `build_driver()` gated with `#[cfg(target_os = "linux")]` |
| 34 | Ensure `cargo test --workspace` passes on Linux after Phase 3 | Incomplete | Full workspace test suite on `ubuntu-latest` CI runner | N/A | Validation gate |
| 35 | Run Linux live smoke tests for rust-chatbot and GPUI scenarios | Incomplete | All ignored live tests pass with `AUTO_UI_RUN_LIVE_TESTS=1` | N/A | Validation gate |

---

## Phase 4 — macOS Driver

| No. | Task | Status | Required Tests Before Complete | Required Documentation Before Complete | Crate Design |
|-----|------|--------|-------------------------------|----------------------------------------|--------------|
| 36 | Create `crates/auto-ui-driver-macos/` crate | Complete | `cargo check` for new crate passes; workspace membership test | Crate-level README explaining macOS-only driver | Add to workspace `Cargo.toml`; create `Cargo.toml`, `src/lib.rs` |
| 37 | Add CoreGraphics and Accessibility dependencies to `auto-ui-driver-macos` | Complete | Lockfile resolution test | Cargo.toml comments for each dep with purpose | `core-graphics`, `core-foundation` dependencies added; Accessibility crate deferred to later tasks |
| 38 | Implement `MacOsWindowDriver` struct and `impl WindowDriver for MacOsWindowDriver` stub | Complete | Compile test asserting `MacOsWindowDriver: WindowDriver` | rustdoc on struct | `pub struct MacOsWindowDriver;` in `src/lib.rs` |
| 39 | Implement `check_required_tools` for macOS (Accessibility permission check) | Complete | Unit test with mock enabling/disabling of accessibility status; error message must contain "System Settings" | rustdoc explaining macOS permission requirement | `AXIsProcessTrustedWithOptions` via ApplicationServices framework; returns descriptive `bail!` if false |
| 40 | Implement `find_windows` using `CGWindowListCopyWindowInfo` | Complete | Unit test filtering by title substring against a known set of window dictionaries; test empty results | Method rustdoc with CoreGraphics API reference | Iterate `CFArray` of `CFDictionary`; match `kCGWindowName` against substring |
| 41 | Implement `find_windows_for_pid` using `CGWindowListCopyWindowInfo` | Complete | Unit test filtering by `kCGWindowOwnerPID`; test PID mismatch | Method rustdoc | Same API as task 40 but filter on `kCGWindowOwnerPID` |
| 42 | Implement `get_geometry` using Accessibility `AXUIElement` | Complete | Test with a known AXUIElement mock or integration test against a real Safari/Terminal window | rustdoc noting that window must be accessibility-visible | Implemented via AppleScript `System Events` as best-effort; parses `{{x, y}, {w, h}}` output |
| 43 | Implement `resize` using Accessibility `AXUIElementSetAttributeValue` | Complete | Test asserting geometry change or proper error if window denies resize | rustdoc with caveat about resizable windows | Implemented via AppleScript `set size of window 1 to {w, h}` |
| 44 | Implement `set_geometry` (resize + move) using Accessibility | Complete | Same test coverage as resize plus position verification | rustdoc | Implemented via AppleScript `set position to {x, y} and set size to {w, h}` |
| 45 | Implement `activate` using `NSRunningApplication` or Accessibility `kAXMainAttribute` | Complete | Test that activation brings window to foreground or returns error | rustdoc | Implemented via AppleScript `set frontmost of process to true` |
| 46 | Implement `lower` using Accessibility or AppleScript | Complete | Test verifying window is sent behind others | rustdoc explaining macOS has no strict z-order "below" and that behavior is best-effort | No-op: macOS has no reliable "lower window" primitive |
| 47 | Implement `background` (lower + restore previous active window) | Complete | Test with two window IDs: assert target is lowered and previous window is activated | rustdoc | Compose `lower` (no-op) and `activate` (restore window) |
| 48 | Implement `screenshot` using `CGWindowListCreateImage` | Complete | Integration test capturing a known window and writing PNG; verify file exists and is valid PNG | rustdoc with window ID format (CGWindowNumber as string) | `CGWindowListCreateImage` + BGRA→RGBA conversion + `image` crate PNG save |
| 49 | Implement `press_key` using `CGEventPost` | Complete | Unit test posting a known key and verifying event creation does not panic; integration test with text field | rustdoc with key-name mapping table and accessibility permission warning | `CGEventSource::newHIDSystemState()`; `CGEvent::new_keyboard_event(source, key_code, true)`; `CGEventPost(tap_location, event)` |
| 50 | Implement `type_text` using `CGEventPost` sequence | Complete | Test typing a string into a focused text field | rustdoc explaining delay between keystrokes | Uses `CGEvent::set_string` for efficient Unicode text entry |
| 51 | Implement `clear_search` and `select_session` as composed methods | Complete | Test that `clear_search` sends Ctrl+A then BackSpace; `select_session` sends Ctrl+F, types, presses Return | rustdoc | Reuse `press_key` and `type_text` |
| 52 | Delegate `crop_metric` and `image_size` to `auto-ui-core` image processing | Complete | Test delegation path on macOS using sample images | rustdoc noting these are platform-agnostic | `auto_ui_core::image_processing::crop_metric(...)` |
| 53 | Implement `window_exists` using `CGWindowListCopyWindowInfo` or AXUIElement | Complete | Test with invalid window ID returning false; test with valid window returning true | rustdoc | Query by window ID and check result array non-empty |
| 54 | Implement `get_active_window` using `CGWindowListCopyWindowInfo` with `kCGWindowListOptionOnScreenOnly` + `kCGWindowListExcludeDesktopElements` | Complete | Test returns a window ID when run from a GUI context | rustdoc | Filter for `kCGWindowLayer == 0` and `kCGWindowIsOnscreen == 1`; return top-most window ID |
| 55 | Add `#[cfg(target_os = "macos")]` conditional compilation for `auto-ui-driver-macos` | Complete | Compile test on `macos-latest` CI runner | Module rustdoc | `#![cfg(target_os = "macos")]` at top of `lib.rs`; crate is empty on non-macOS |
| 56 | Add `#[cfg(target_os = "macos")]` dependency in `auto-ui-cli/Cargo.toml` | Complete | `cargo check --workspace` on macOS resolves `auto-ui-driver-macos` | Cargo.toml comment | `[target.'cfg(target_os = "macos")'.dependencies]` block added |
| 57 | Update `auto-ui-cli` `build_driver()` to instantiate `MacOsWindowDriver` on macOS | Complete | Compile test on macOS asserting `MacOsWindowDriver` is constructed | CLI rustdoc for driver selection logic | `#[cfg(target_os = "macos")]` branch returning `Box::new(MacOsWindowDriver::new())` |
| 58 | Update `ensure_display` in `auto-ui-core` to be platform-aware | Complete | Unit test: on Linux, errors when `DISPLAY` missing; on macOS, errors when `SSH_CONNECTION` present; succeeds otherwise | Function rustdoc explaining platform differences | `#[cfg(target_os = "linux")]` check `DISPLAY`; `#[cfg(target_os = "macos")]` check `SSH_CONNECTION` |
| 59 | Gate `--headless` to error on macOS in `auto-ui-cli/src/run_cmd.rs` | Incomplete | Unit test: `Args { headless: true, ... }` on macOS returns error containing "not supported" | CLI help text must note `--headless` is Linux-only | `if args.headless { #[cfg(target_os = "macos")] bail!("..."); }` |
| 60 | Ensure `cargo check --workspace` passes on macOS | Incomplete | CI compile check on `macos-latest` | N/A | Validation gate |
| 61 | Ensure `cargo test --workspace` passes on macOS (unit tests only, no live tests) | Incomplete | CI test run on `macos-latest` excluding ignored live tests | N/A | Validation gate |

---

## Phase 5 — Validation, CI, and Documentation

| No. | Task | Status | Required Tests Before Complete | Required Documentation Before Complete | Crate Design |
|-----|------|--------|-------------------------------|----------------------------------------|--------------|
| 62 | Add `macos-latest` job to `.github/workflows/ci.yml` | Incomplete | CI workflow parses correctly (use `actionlint` or GitHub Actions schema validation); macOS job completes successfully | CONTRIBUTING.md must mention macOS CI | Duplicate existing `test` job with `runs-on: macos-latest`; run `cargo fmt`, `cargo clippy`, `cargo check`, `cargo test`, `cargo build --release` |
| 63 | Update `README.md` requirements section for macOS | Incomplete | N/A (docs-only) | README must list macOS 12+ and Accessibility permissions; remove "Linux only" language | New section: "Requirements — macOS" |
| 64 | Update `README.md` setup instructions to remove ImageMagick | Incomplete | N/A (docs-only) | Linux setup must remove ImageMagick install commands; macOS setup must not mention it | Edit existing apt-get / brew lists |
| 65 | Update `DESIGN.md` cross-platform section | Incomplete | N/A (docs-only) | DESIGN must state that driver expansion now includes macOS; update Non-Goals if cross-platform parity is now a goal | Edit "Future Work" and "Non-Goals" sections |
| 66 | Update `REMAINING.md` to remove or reclassify macOS driver items | Incomplete | N/A (docs-only) | Any macOS-related backlog items must be marked completed or moved to this task list | Edit `REMAINING.md` |
| 67 | Update `REPORT_SCHEMA.md` if image metric field semantics changed | Incomplete | N/A (docs-only) | If `VisualMetric` serialization changed, schema must be updated | Validate schema against actual report output |
| 68 | Add macOS-specific troubleshooting entry in `docs/troubleshooting.md` | Incomplete | N/A (docs-only) | Troubleshooting must cover: Accessibility permission denied, window not found due to sandboxing, `--headless` error | New section: "macOS Issues" |
| 69 | Add macOS live smoke test instructions to `README.md` | Incomplete | N/A (docs-only) | Document env vars and setup for running ignored tests on macOS | Same format as existing Linux live-test section |
| 70 | Add `docs/macos-support-design.md` cross-reference to `DESIGN.md` | Incomplete | N/A (docs-only) | DESIGN.md should link to the macOS design doc for implementation details | Add link in "Future Work" or "Driver Expansion" section |
| 71 | Final end-to-end validation: run rust-chatbot `debug` scenario on macOS with live test | Incomplete | Live smoke test with `AUTO_UI_RUN_LIVE_TESTS=1` on macOS hardware | N/A | Manual validation gate |
| 72 | Final end-to-end validation: run GPUI `scroll_matrix` scenario on macOS with live test | Incomplete | Live smoke test with `AUTO_UI_RUN_LIVE_TESTS=1` on macOS hardware | N/A | Manual validation gate |
| 73 | Run `cargo clippy --workspace --all-targets` on macOS with zero warnings | Incomplete | CI clippy job on `macos-latest` passes | N/A | Quality gate |
| 74 | Run `cargo fmt --all -- --check` on macOS | Incomplete | CI fmt job on `macos-latest` passes | N/A | Quality gate |
